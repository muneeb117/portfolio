Save these real image files here (drag from chat → Finder, or right-click → Save Image):

  muneeb.jpg                     — your headshot (square works best)
  screenshots/revenuecat.png     — RevenueCat overview
  screenshots/appsflyer.png      — AppsFlyer dashboard
  screenshots/googleads.png      — Google Ads overview

Once these files exist here, they appear on the site automatically.
Until then, the site shows a clean fallback (no broken images).
