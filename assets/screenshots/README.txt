Drop your REAL dashboard screenshots here with these exact names:

  revenuecat.png   — RevenueCat overview (MRR, subs, customers, charts)
  appsflyer.png    — AppsFlyer dashboard (ROAS, installs, revenue)
  googleads.png    — Google Ads overview (clicks, CPC, conversions)

Optional extras (any name, .png/.jpg):
  fiverr-profile.png, upwork-profile.png, app-*.png (in-app screenshots)

I'll embed them 1:1 inside a clean browser/phone frame on the matching pages.
